# HJTメソッド™学習コンテンツ セットアップガイド

このサイトを本番公開するまでの手順をまとめています。
専門知識がなくても進められるよう、順番どおりに書いています。

---

## 全体像(3つの無料サービスを組み合わせます)

| 役割 | サービス | 費用 |
|---|---|---|
| サイトの公開 | GitHub Pages | 無料 |
| 会員情報・進捗の保管 | Supabase | 無料 |
| 動画の置き場 | YouTube(限定公開) | 無料 |

> まだ何も設定しなくても、サイトは「お試しモード」で動きます。
> お試しモードでは、データはお使いのブラウザの中だけに保存されます。
> 見た目や操作感の確認に使ってください。
>
> - 受講生でログイン: `member@example.com` / `hjt2026demo`
> - 運営者でログイン: `admin@example.com` / `hjt2026admin`
> - 初回パスワード登録の体験: `first@example.com`(パスワード未設定)

---

## ステップ1: Supabase(データの保管先)の準備

1. https://supabase.com を開き、「Start your project」から無料登録します(GitHubアカウントでそのまま登録できます)。
2. 「New project」で新しいプロジェクトを作ります。
   - Name: `hjt-learning` など好きな名前
   - Database Password: 自動生成のままでOK(どこかに控えておく)
   - Region: `Northeast Asia (Tokyo)` を選ぶと速いです
3. プロジェクトができたら、左メニューの **SQL Editor** を開きます。
4. この資料と同じ場所にある `supabase/setup.sql` ファイルをメモ帳などで開き、
   **一番下の `your-email@example.com` をあなたのメールアドレスに書き換えて**から、
   全文をコピーして SQL Editor に貼り付け、「Run」を押します。
   - 「Success」と出れば完了です。
5. **メール確認をオフにする**(これをしないと初回パスワード登録がすぐに完了しません)
   - 左メニュー **Authentication** → **Sign In / Providers**(または Settings)→ **Email** を開く
   - 「**Confirm email**」のスイッチを **オフ** にして保存
6. **接続情報を控える**
   - 左メニューの歯車(Project Settings)→ **API**(または Data API)を開く
   - 「**Project URL**」と「**anon public**(publishable)キー」の2つをコピーして控えます

## ステップ2: 自分のパソコンで本番モードを確認(任意)

1. `hjt-learning` フォルダにある `.env.example` をコピーして、名前を `.env` に変えます。
2. `.env` をメモ帳で開き、ステップ1-6で控えた2つの値を貼り付けます。

```
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...(長い文字列)
```

3. サイトを表示し直すと「お試しモード」の帯が消え、本番の保管先につながります。
4. ログイン画面の「初回パスワード登録」で、setup.sql に書いたあなたのメールアドレスと
   好きなパスワード(半角英数字8〜12文字)を登録すると、管理画面に入れます。

## ステップ3: GitHubで公開する

1. https://github.com で新しいリポジトリ(サイトの置き場)を作ります。
   - 名前: `hjt-learning` など
   - **Private ではなく Public** を選びます(無料プランのGitHub Pagesに必要)
2. このフォルダの中身をGitHubに送ります(Claudeに「GitHubに公開して」と頼めば代行します)。
3. GitHubのリポジトリ画面で **Settings → Secrets and variables → Actions** を開き、
   「New repository secret」で次の2つを登録します(ステップ1-6で控えた値)。
   - 名前 `VITE_SUPABASE_URL` → Project URL を貼り付け
   - 名前 `VITE_SUPABASE_ANON_KEY` → anon キーを貼り付け
4. **Settings → Pages** を開き、「Build and deployment」の Source を **GitHub Actions** にします。
5. 以後、変更をGitHubに送るたびに自動でサイトが更新されます。
   公開先の住所(URL)は `https://あなたのユーザー名.github.io/リポジトリ名/` です。

> メモ: anon キーは「サイトに埋め込む前提の公開用キー」です。
> 会員の情報は Supabase 側の閲覧制限(各自の受講コースしか見られない設定)で守られています。

## ステップ4: 日々の運営のしかた

### 動画を追加する

1. 動画をYouTubeにアップロードし、公開設定を「**限定公開**」にします。
2. 動画のリンク(URL)をコピーします。
3. 学習サイトに運営者でログイン → 右上「管理画面」→「コンテンツ管理」
4. ①コースを選び(なければ登録)、②テーマを作り、③動画タイトルを入力、
   ④サムネイル画像をドラッグ&ドロップ(任意)、⑤YouTubeのリンクを貼り付けて登録します。

### 会員を追加する

1. 管理画面 →「会員管理」
2. ①会員名、②メールアドレス、③受講コースを選んで登録します。
3. 会員に「サイトのURL」と「登録したメールアドレス」を伝え、
   「初回パスワード登録」からパスワード(半角英数字8〜12文字)を設定してもらいます。
4. 会員一覧で、各会員の初回登録の状況とコースごとの進捗(%)が確認できます。

---

## 知っておいていただきたいこと

- **YouTube限定公開**は「リンクを知っている人だけが見られる」方式です。サイトのログインで実質的に守られますが、リンクそのものが外部に出回ると視聴される可能性はあります(有料スクールでも広く使われている、一般的な水準の守り方です)。
- **無料枠の目安**: Supabaseの無料枠は会員5万人・データ500MB・画像1GBまで。今回の規模(4〜5コース)なら十分です。
- 会員を削除すると、その方の学習記録も消えます。
